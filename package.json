{
  "name": "dawn-duel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A symmetric 1v1 midlane action game for Usion.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "dev:multiplayer": "node scripts/dev-multiplayer.mjs",
    "smoke:multiplayer": "node scripts/smoke-multiplayer.mjs",
    "build:server": "node scripts/build-server.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run build:server && node scripts/check-assets.mjs && node scripts/check-sdk.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "jose": "^6.1.0",
    "ws": "^8.18.3"
  }
}
